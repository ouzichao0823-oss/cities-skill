# User's Zotero path (local only, not shared)
corpus/zotero-path.txt

# User's research constitution (personal, not shared)
constitution.md.active

# OS files
.DS_Store
Thumbs.db

# Archive snapshots (not tracked)
*.rar

# Local model config (generated by install script, not shared)
model-config.md
